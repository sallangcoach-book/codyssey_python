# 프로젝트 파일 구조 (structure.md)

이 문서는 `quiz_game` 프로젝트의 각 파일이 무엇을 담당하는지 설명합니다.


```
quiz_game/
├── main.py          # 프로그램 시작점 (실행 파일)
├── quiz.py          # Quiz 클래스 (문제 1개를 표현)
├── quiz_game.py      # QuizGame 클래스 (게임 전체 진행, 저장/불러오기)
├── state.json        # 퀴즈 데이터 + 점수 저장 파일 (실행하면 자동 생성됨)
├── README.md         # 프로젝트 설명서 겸 학습 가이드
├── structure.md       # (지금 보고 있는 이 파일) 폴더 구조 설명
└── .gitignore         # Git이 추적하지 않을 파일 목록
```



| 파일 | 역할 | 비유 |
|---|---|---|
| `main.py` | 프로그램을 켜는 스위치 역할만 합니다. | 리모컨의 "전원" 버튼 |
| `quiz.py` | 퀴즈 문제 "한 장"이 어떻게 생겼는지 정의합니다. | 시험지 한 장의 양식 |
| `quiz_game.py` | 문제들을 모아서 실제로 게임을 진행하고, 파일에 저장합니다. | 시험을 진행하는 감독관 |


1. **읽기 쉬움**: 파일 이름만 봐도 어떤 코드가 들어있는지 짐작할 수 있습니다.
2. **수정하기 쉬움**: 문제 형식을 바꾸고 싶으면 `quiz.py`만 보면 됩니다.
3. **재사용하기 쉬움**: `quiz.py`의 Quiz 클래스는 나중에 다른 프로그램(예: 웹 퀴즈 앱)에서도 그대로 가져다 쓸 수 있습니다.

## 데이터 흐름 한눈에 보기

```
[main.py]
    │  QuizGame 객체를 만들고 실행(run)
    ▼
[quiz_game.py - QuizGame 클래스]
    │  Quiz 객체들을 리스트로 관리
    │  메뉴 선택에 따라 기능 실행
    │  state.json 파일을 읽고 씀
    ▼
[quiz.py - Quiz 클래스]
    문제 1개(질문, 선택지, 정답, 힌트)를 표현
```

## state.json

`state.json`은 `python main.py`를 처음 실행하면, `quiz_game.py`의 `save_data()` 메서드가
자동으로 이 파일을 만들어 줍니다. 그 이후부터는 프로그램을 종료했다가 다시 실행해도 저장된 퀴즈와 점수가 그대로 남아 있습니다.
